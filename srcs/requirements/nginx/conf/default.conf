# =============================================================================
# NGINX — Virtual host for login.42.fr
# =============================================================================
# PURPOSE: TLS termination + FastCGI pass to wordpress:9000
#
# REQUIREMENTS:
#   - listen 443 ssl;
#   - ssl_protocols TLSv1.2 TLSv1.3;  (ONLY these)
#   - server_name your_login.42.fr;     (replace with DOMAIN_NAME)
#   - root must match WordPress path INSIDE wordpress container
#     → nginx does NOT mount WP volume; proxy PHP only OR use same root path
#
# ARCHITECTURE NOTE:
#   Common pattern: nginx only proxies PHP to wordpress:9000 with
#   fastcgi_param SCRIPT_FILENAME pointing to /var/www/html/... on WP container.
#   Static files can be served by nginx if you mount wordpress volume in nginx too
#   (subject allows — both volumes are named; evaluators may ask why you chose your way).
#
# TODO: Complete server block
# =============================================================================

# server {
#   listen 443 ssl;
#   server_name your_login.42.fr;
#
#   ssl_certificate     /etc/nginx/ssl/nginx.crt;
#   ssl_certificate_key /etc/nginx/ssl/nginx.key;
#   ssl_protocols       TLSv1.2 TLSv1.3;
#
#   root /var/www/html;
#   index index.php;
#
#   location / {
#     try_files $uri $uri/ /index.php?$args;
#   }
#
#   location ~ \.php$ {
#     fastcgi_pass wordpress:9000;
#     fastcgi_index index.php;
#     include fastcgi_params;
#     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#   }
# }
