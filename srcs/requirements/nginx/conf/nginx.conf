# =============================================================================
# NGINX — Main configuration
# =============================================================================
# PURPOSE: Global nginx settings, include conf.d/*.conf
#
# TODO: Set worker_processes, events, http block
# TODO: Include mime.types, set client_max_body_size for WP uploads
# =============================================================================

# user www-data;
# worker_processes auto;
# events { worker_connections 1024; }
# http {
#   include /etc/nginx/mime.types;
#   include /etc/nginx/conf.d/*.conf;
# }
