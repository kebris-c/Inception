# Shared tools / scripts (optional)

Place helper scripts used across services here if needed.

Example: `wait-for-it.sh` for database readiness (vendor or write your own).

**Note:** Subject requires you understand every script — avoid blind copy-paste.
