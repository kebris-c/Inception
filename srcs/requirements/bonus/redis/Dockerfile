# =============================================================================
# Redis — Bonus Dockerfile (optional)
# =============================================================================
# PURPOSE: Cache layer for WordPress (object cache drop-in or plugin).
# TODO: Build from Alpine/Debian, install redis-server
# TODO: CMD redis-server with foreground mode
# =============================================================================

# FROM alpine:3.19
# RUN apk add --no-cache redis
# CMD ["redis-server", "--bind", "0.0.0.0"]
