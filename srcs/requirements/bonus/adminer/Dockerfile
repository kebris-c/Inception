# =============================================================================
# Adminer — Bonus Dockerfile (optional)
# =============================================================================
# PURPOSE: Web DB admin — do NOT expose MariaDB port; proxy via nginx or dedicated port.
# TODO: Build image with adminer + php — custom Dockerfile required (no pull adminer image)
# =============================================================================
