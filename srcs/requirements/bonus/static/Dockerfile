# =============================================================================
# Static website — Bonus Dockerfile (optional)
# =============================================================================
# PURPOSE: Simple site in any language EXCEPT PHP (e.g. nginx serving static HTML).
# TODO: Separate container, separate content volume or baked-in HTML
# =============================================================================
