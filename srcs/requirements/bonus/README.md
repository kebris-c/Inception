# =============================================================================
# Bonus services — optional (evaluated only if mandatory is perfect)
# =============================================================================
# Each bonus = own Dockerfile + own container + optional volume.
# You may expose extra ports for bonus (FTP, static site, Adminer, etc.)
#
# Uncomment and wire in docker-compose.yml when ready.
# =============================================================================

# Available bonus ideas from subject:
#   - redis/     → WordPress object cache
#   - ftp/       → vsftpd on wordpress volume
#   - static/    → small site (not PHP)
#   - adminer/   → DB web UI
#   - custom/    → your choice — justify at defense
