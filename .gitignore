# =============================================================================
# Inception — Git ignore
# =============================================================================
# CRITICAL: Committing passwords = automatic project failure.
# Only files required by the 42 subject should be tracked.
# =============================================================================

# --- Local study / subject files (not for 42 submission) ---
INCEPTION_GUIDE.md
SUBJECT.md
en.subject.pdf

# --- Environment (create from .env.example on VM) ---
srcs/.env

# --- Secrets (create locally on VM only) ---
secrets/*.txt
secrets/*
!secrets/.gitkeep
!secrets/README.md

# --- TLS private keys ---
**/ssl/*.key
**/ssl/*.pem

# --- WordPress runtime ---
**/wp-config.php

# --- OS / editor ---
.DS_Store
*.swp
*~
.idea/
.vscode/

# --- Docker local overrides ---
docker-compose.override.yml
